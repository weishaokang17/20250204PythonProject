# code01.py

**主要内容**

+ 字符串的大小写转换
+ 字符串的分隔，split()方法
+ 字符串的count()方法
+ 字符串的find()方法和index()方法
+ 字符串的startwith()、endwith()方法


* * *

# code02.py
**主要内容**

+ 字符串的替换，replace()方法
+ 字符串的center()方法
+ 字符串的strip()、lstrip()、rstrip()方法

* * *

# code03.py

**主要内容**

+ 字符串的格式化输出 -- % 这种方法
+ 字符串的格式化输出 -- format()方法
+ 字符串的格式化输出 -- f-string

* * *
# code04.py
**主要内容**

+ 字符串格式化输出的一些格式设定
> 这个语法说明，详见obsidian的笔记中，有详细说明

* * *

# code05.py

**主要内容**

+ 编码  str->bytes
+ 编码中的出错问题，三种取值 strict replace  ignore
+ 解码

* * *

# code06.py

**主要内容**

+ str的 isdigit()方法
+ str的 isnumeric()方法
+ str的 isalpha()方法
+ str的 isalnum()方法
+ str的 islower()方法
+ str的 isupper()方法
+ str的 istitle()方法
+ str的 isspace()方法

* * *

# code07.py

**主要内容**
+ 字符串的拼接 -- 使用 + 号拼接
+ 字符串的拼接 -- 使用join()拼接
+ 字符串的拼接 -- 直接拼接
+ 字符串的拼接 -- 使用格式化字符串拼接


* * *

# code08.py

**主要内容**

+ 字符串的去重，几种去重方法

* * *